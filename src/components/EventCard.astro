---
const { title, year, img, slug } = Astro.props;
---

<a href={`/events/${slug}`}>
	<img src={img} />
	<h2 class='text-gray-700 text-xl pt-1'>{title}</h2>
	<h3 class='text-gray-500 text-m'>{year}</h3>
</a>
